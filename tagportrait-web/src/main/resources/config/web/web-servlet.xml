<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc-3.1.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.1.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.1.xsd">
    <!--注解支持@RequestMapping, @Controller-->
    <mvc:annotation-driven/>
    <!--自动扫描包-->
    <!--<context:component-scan base-package="com.ideal.manage"/>-->
    <!--Spring MVC最打击新人的事情，你必须保证spring-mvc.xml的<context:component-scan>只扫描Controller，
    而 applicationContext.xml里的不包含Controller。
    否则你定义在applicationContext.xml里的事务就要失效了-->
    <context:component-scan base-package="com.ideal.tagportrait.web.controller">
        <context:include-filter type="annotation" expression="org.springframework.stereotype.Controller"/>
    </context:component-scan>

    <!--framework 拦截器拦截所有请求-->
    <mvc:interceptors>
        <bean class="com.ideal.tagportrait.web.BeeUIInterceptors"/>
        <bean class="org.springframework.orm.jpa.support.OpenEntityManagerInViewInterceptor">
            <property name="entityManagerFactory" ref="entityManagerFactory"/>
        </bean>
    </mvc:interceptors>

    <!--视图解析器-->
    <bean id="viewResolver" class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/jsp/"/> <!-- 前缀 -->
        <property name="suffix" value=".jsp"/> <!-- 后缀 -->
    </bean>

    <!--所有异常的统一处理-->
    <bean id="handlerExceptionResolver" class="com.ideal.tagportrait.web.UIHandlerExceptionResolver"/>

</beans>